spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/MiniposterAuthDatabase
    username: miniposter
    password: miniposter_password
    driverClassName: org.postgresql.Driver
    continue-on-error: true
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  security:
    user:
      name: user
      password: password

server:
  port: 8001

rsa:
  publicKeyLocation: "auth-server/public_key"
  privateKeyLocation: "auth-server/private_key"
